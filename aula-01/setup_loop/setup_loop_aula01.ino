  void setup() {
    // No void setup() colocaremos as ações que
    // queremos que aconteça apenas uma vez.

    // <-- As duas barras indicam que essa linha
    // é um comentário, ou seja, não é uma ação.
  }

  void loop() {
    /* No void loop() colocaremos as ações que
    queremos que sejam repetidas indefinidamente */
    
    /* Também podemos fazer blocos de comentários 
    começando com "/*" e terminando com --> */
  }
  